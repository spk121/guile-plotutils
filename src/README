These files were taken verbatim from GNU Plotutils.

There are three conditional compilations
- HAVE_STRERROR
- INCLUDE_PNG_SUPPORT
- X_DISPLAY_MISSING

For HAVE_STRERROR, we can test this easily enough. For the latter two,
there is no easy way to infer if a user's install of libplot has
these.
